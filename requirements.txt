# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# LangChain core and components
langchain==0.1.0
langchain-community==0.0.10
langchain-chroma==0.1.4
langchain-huggingface==0.0.3

# Vector database (pinned to avoid onnxruntime conflict)
chromadb==0.4.17

# Embeddings and ML
sentence-transformers==2.6.1
huggingface-hub==0.23.1
torch==2.2.0
transformers==4.40.0

# PDF processing - without unstructured-inference to avoid conflict
PyMuPDF==1.23.8
pypdf==3.17.4
# Note: Install unstructured separately with: pip install unstructured[pdf]==0.11.8 --no-deps

# HTTP and API
httpx==0.25.2
requests==2.31.0

# Environment and utilities
python-dotenv==1.0.0
pydantic==2.5.0

# Session management and utilities for conversation support
# uuid # Built-in Python module, no installation needed
# datetime # Built-in Python module, no installation needed

# Optional: OpenAI SDK
openai==1.3.8

# Additional dependencies for enhanced conversation features
typing-extensions==4.8.0  # For better type hints support
python-multipart==0.0.6   # For handling form data if needed in frontend

# --- NEW DEPENDENCIES FOR ENHANCED AI AGENT ---
# spaCy for advanced NLP (Named Entity Recognition, dependency parsing)
spacy==3.7.2
# You also need to download the English model after installing spacy:
# python -m spacy download en_core_web_sm

# NumPy is often a dependency but might be included by others. Explicitly listing it ensures compatibility.
numpy==1.26.4
# --- END NEW DEPENDENCIES ---

# Development and debugging (optional)
# pytest==7.4.3
# pytest-asyncio==0.21.1
