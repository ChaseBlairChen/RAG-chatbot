# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0

# LangChain core and components (Let pip find compatible versions)
langchain # Remove ==0.1.0
langchain-community # Remove ==0.0.10
langchain-chroma==0.1.4
langchain-huggingface==0.0.3

# Vector database (pinned to avoid onnxruntime conflict)
chromadb==0.4.17

# Embeddings and ML
sentence-transformers==2.6.1
huggingface-hub==0.23.1
torch==2.2.0
transformers==4.40.0

# PDF processing - without unstructured-inference to avoid conflict
PyMuPDF==1.23.8
pypdf==3.17.4
# Note: Install unstructured separately with: pip install unstructured[pdf]==0.11.8 --no-deps

# HTTP and API
httpx==0.25.2
requests==2.31.0

# Environment and utilities
python-dotenv==1.0.0
pydantic==2.5.0

# Optional: OpenAI SDK
openai==1.3.8

# Additional dependencies for enhanced conversation features
typing-extensions==4.8.0
python-multipart==0.0.6

# --- NEW DEPENDENCIES FOR ENHANCED AI AGENT ---
spacy==3.7.2
numpy==1.26.4
# --- END NEW DEPENDENCIES ---
